# -------------------------------------------------------
# Variables públicas del Frontend (Astro / Vite)
# Los nombres PUBLIC_* son expuestos al cliente (browser).
# -------------------------------------------------------

# URL base del backend Flask, incluyendo el prefijo /api.
# Ejemplo producción: https://api.campingarequita.uy/api
# Ejemplo local:      http://localhost:5000/api
#
# En Coolify, configurar como Build Arg antes de cada despliegue.
# En Docker manual: --build-arg PUBLIC_API_URL=https://...
PUBLIC_API_URL=http://localhost:5000/api
